version: '3'
services:

  nginx:
    build: ./stretch
    image: khs1994/nginx:1.13.8-tls1.3-stretch
    ports:
      - 443:443
      - 80:80
    volumes:
      - ./conf.d:/etc/nginx/conf.d:ro
      - ./app:/app/test:rw
